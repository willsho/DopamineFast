import SwiftUI

extension Color {
    static let appBackground = Color(red: 0.96, green: 0.97, blue: 0.96)
    static let cardBackground = Color(uiColor: .secondarySystemBackground)
    static let tileBackground = Color(uiColor: .tertiarySystemBackground)
}
